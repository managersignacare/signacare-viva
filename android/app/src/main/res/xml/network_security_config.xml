<?xml version="1.0" encoding="utf-8"?>
<!--
  Viva (patient mobile) — network security config (S8.2).

  Play Store submission requires HTTPS-only for public domains. Viva
  talks to a single cloud API + optional on-prem gateway; both must be
  TLS. Cleartext is permitted only to localhost / loopback / the
  Android emulator host alias for local development.
-->
<network-security-config>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
    </domain-config>

</network-security-config>
